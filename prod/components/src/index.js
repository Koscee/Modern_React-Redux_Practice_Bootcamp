import React from "react";
import ReactDOM from "react-dom";
import faker from "faker";

const App = () => {
    return (
        <div className="ui container comments">
            {/* previous code moved to CommentDetail.js */}
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));